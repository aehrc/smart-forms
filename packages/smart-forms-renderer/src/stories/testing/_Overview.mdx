import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Testing/Overview" />

# Testing

The below stories are used for internal testing purposes.

They can be used as reference for certain functionalities, but are not intended to be a showcase of features.

Some ValueSets might be outdated, which causes certain choice/open-choice questions to not render correctly.
