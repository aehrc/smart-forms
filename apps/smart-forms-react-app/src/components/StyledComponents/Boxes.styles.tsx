import { Box, styled } from '@mui/material';

export const MainGridContainerBox = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  padding: '24px',
  height: '100%',
  gap: 12
}));
