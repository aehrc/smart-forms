---
sidebar_position: 5
---

import IframeContainer from '../../../src/react/IframeContainer';

# Value Constraints

This subsection consists of extensions related to value constraints and form validation.

The elements/extensions that fall under this subsection are:

**From Core Questionnaire**

- [required](https://www.hl7.org/fhir/r4/questionnaire-definitions.html#Questionnaire.item.required)

**From SDC**

- [maxLength](http://hl7.org/fhir/uv/sdc/behavior.html#maxLength)
- [minLength](http://hl7.org/fhir/uv/sdc/behavior.html#minLength)
- [regex](http://hl7.org/fhir/uv/sdc/behavior.html#regex)
- [minValue](http://hl7.org/fhir/uv/sdc/behavior.html#minValue)
- [maxValue](http://hl7.org/fhir/uv/sdc/behavior.html#maxValue)
- [minQuantity](http://hl7.org/fhir/uv/sdc/behavior.html#minQuantity)
- [maxQuantity](http://hl7.org/fhir/uv/sdc/behavior.html#maxQuantity)
- [maxDecimalPlaces](http://hl7.org/fhir/uv/sdc/behavior.html#maxDecimalPlaces)
- [targetConstraint](http://hl7.org/fhir/extensions/5.3.0-ballot-tc1/StructureDefinition-targetConstraint.html)

:::tip
Elements/extensions in this subsection invokes form validation in the renderer. OperationOutcome resources are generated when the current state of the form does not meet the constraints defined in the Questionnaire.

For more information, refer to the [InvalidItems](/docs/dev/storehooks/questionnaire-response-store/invalid-items) section in Developer Usage.

:::

### Required

An indication, if true, that the item must be present in a "completed" QuestionnaireResponse. If false, the item may be skipped when answering the questionnaire.

Also appears in the [Fundamental Elements](/docs/sdc/advanced/fundamental) subsection.

#### Basic Usage

<IframeContainer storyId="sdc-9-1-4-rendering-other--required" initialHeight={150} />

### MaxLength

The maximum length of the string value of the answer to this question.

MaxLength is only supported on **decimal**, **integer**, **string** and **text** "text-field"-based items.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-1-behavior-value-constraints--max-length"
  initialHeight={260}
/>

### MinLength

The minimum length of the string value of the answer to this question.

MinLength is only supported on **decimal**, **integer**, **string** and **text** "text-field"-based items.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-1-behavior-value-constraints--min-length"
  initialHeight={260}
/>

### Regex

A [regular expression](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_expressions) constraint for the string value of the answer to this question.

Regex is only supported on **decimal**, **integer**, **string** and **text** "text-field"-based items.

:::tip

"regex" extension doesn't allow you to customise error messages. It is recommended to use [targetConstraint](#targetConstraint) instead.

:::

#### Basic Usage

<IframeContainer storyId="sdc-10-1-1-behavior-value-constraints--regex" initialHeight={260} />

### MinValue

The minimum value of the quantifiable answer to this question.

MinValue is only supported on **decimal**, **integer**, **date** and **dateTime** items.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-1-behavior-value-constraints--min-value"
  initialHeight={260}
/>

### MaxValue

The maximum value of the quantifiable answer to this question.

MinValue is only supported on **decimal**, **integer**, **date** and **dateTime** items.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-1-behavior-value-constraints--max-value"
  initialHeight={260}
/>

### MinQuantity

The minimum value of the quantifiable `valueQuantity.value` to this question.

MinQuantity is only supported on **quantity** items.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-1-behavior-value-constraints--min-quantity"
  initialHeight={300}
/>

### MaxQuantity

The maximum value of the quantifiable `valueQuantity.value` to this question.

MaxQuantity is only supported on **quantity** items.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-1-behavior-value-constraints--max-quantity"
  initialHeight={300}
/>

### MaxDecimalPlaces

The maximum number of decimal places allowed in the numeric answer to this question.

MaxDecimalPlaces is only supported on **decimal** items.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-1-behavior-value-constraints--max-decimal-places"
  initialHeight={260}
/>

### TargetConstraint

Specifies a constraint that the answer to this question must satisfy, expressed as a FHIRPath expression.

This extension allows you to specify human-readable error messages when the constraint is violated.

#### Basic Usage

<IframeContainer
  storyId="sdc-10-1-4-behavior-other-control--target-constraint-simple"
  initialHeight={260}
/>

#### Multiple Constraints Usage

<IframeContainer
  storyId="sdc-10-1-4-behavior-other-control--target-constraint-multi"
  initialHeight={170}/>
