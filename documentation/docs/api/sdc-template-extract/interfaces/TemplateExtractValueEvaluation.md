# Interface: TemplateExtractValueEvaluation

Result of evaluating a `valueExpression` from the `templateExtractValue` extension.
e.g. `"item.where(linkId = 'family').answer.value.first()"` → `"Doe"`

## Properties

### valueExpression

> **valueExpression**: `string`

***

### valueResult

> **valueResult**: `any`
