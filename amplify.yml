version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - |
              export REACT_APP_SENTRY_RELEASE=`git rev-parse --short HEAD`
              npm run build -w apps/smart-forms-react-app
        postBuild:
          commands:
            - |
              export VERSION=`git rev-parse --short HEAD` && \
              npx sentry-cli releases new $VERSION && \
              npx sentry-cli releases set-commits $VERSION --local --ignore-empty --ignore-missing && \
              npx sentry-cli releases files $VERSION upload-sourcemaps build
      artifacts:
        baseDirectory: apps/smart-forms-react-app/build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*